import { createStore } from "vuex";
import tagNav from "./modules/tagNav";

const state = {
};

const getters = {};
const mutations = {};

const actions = {};

export default createStore({
  modules: {
    tagNav,
  },
  state,
  getters,
  mutations,
  actions,
});
